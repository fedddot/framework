add_library(uart_communication_manager INTERFACE)
target_include_directories(uart_communication_manager INTERFACE ${CMAKE_CURRENT_LIST_DIR}/include)
target_link_libraries(uart_communication_manager INTERFACE communication_manager)

add_subdirectory(tests EXCLUDE_FROM_ALL)